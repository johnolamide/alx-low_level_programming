#include<stdio.h>
/**
 * main - prints alphabet in lower and upper case
 * Retun: returns 0 for SUCCESS
 */
int main(void)
{
	char c;
	char u;

	for (c = 'a'; c <= 'z'; c++)
		putchar(a);

	for (u = 'A'; u <= 'Z'; u++)
		putchar(u);

	putchar('\n');

	return (0);
}
